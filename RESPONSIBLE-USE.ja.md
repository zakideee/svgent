[English](RESPONSIBLE-USE.md) | **日本語**

# 利用上の注意

svgent は利用者が書いた台本を描画します。実セッションを収集したり、モデル・シェル・リポジトリへ接続したりすることはありません。読むのは渡された台本だけです。創作の会話も、実セッションを整理した解説も、どちらも想定した用途です。

## 成果物に残るもの

- すべての書き出しに `simulated=true` が入ります。外す手段はありません。
- 「台本の性質」で選んだ値が `model-kind` として残ります(`fictional` / `reenactment`)。
- 実在製品のロゴや外観は再現しません。svgent は非提携の独立実装です。

## 画像と保存範囲

4 MiB 以下の PNG、JPEG、WebP を、開いているブラウザのタブの中だけで Data URL として扱います。アップロードもアカウントもサーバー保存もありません。自動保存が持つのはテキストと設定だけで、画像が残るのはタブ内と、利用者が保存した台本 JSON だけです。
